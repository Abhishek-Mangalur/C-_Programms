// Addition of two no.

#include <iostream>
using namespace std;
int main()
{
    int a,b,c;          // declaration of variables
    cout << "Enter two numbers: " << endl;
    cin >> a >> b;          // reading the no.
    c = a + b;          // calculating exp
    cout << "The sum is = " << c;       // result
    return 0;
}
